# A template folder makes GitHub offer a chooser, and by default it takes the blank issue away.
# Keep it: the calibration form is one specific errand, and everything else — a printer that won't
# connect, a wrong reset, a question — is worse off squeezed into a form nobody designed for it.
blank_issues_enabled: true
